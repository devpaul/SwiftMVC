proj.name = SwiftMVC

build.version = 0.2.1

build.name = ${proj.name}_${build.version}

build.src.folder = ${basedir}/src

build.libs.folder = ${basedir}/libs

build.dst.filename = ${build.name}.swc

build.dst.folder = ${basedir}/build

build.config.filename = flex-config.xml

build.config.template = ${basedir}/setup/build/TEMPLATE-${build.config.filename}

docs.dst.folder = ${build.dst.folder}/docs